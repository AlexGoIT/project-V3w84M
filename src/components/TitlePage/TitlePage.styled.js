import styled from '@emotion/styled';

export const Title = styled.h1`
  margin-top: 72px;
  font-family: Roboto;
  color: #efede8;
  font-size: 32px;
  font-style: normal;
  font-weight: 700;
  line-height: 1.375;
`;
